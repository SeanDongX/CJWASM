// L1 std.argopt 全量 API 测试
// 类型：ArgOpt (Deprecated), ArgumentSpec, ParsedArguments 等
// 函数：parseArguments(args, specs): ParsedArguments
package std_examples

import std.argopt

// API 列表（vendor）:
// - @Deprecated public class ArgOpt: init(shortArgFormat), init(longArgList), init(shortArgFormat, longArgList), ...
// - public func parseArguments(args: Array<String>, specs: Array<ArgumentSpec>): ParsedArguments
// - ArgumentSpec, ParsedArguments 等
func api_std_argopt_used(): Unit {
    ()
}
